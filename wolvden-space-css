/* soft space den theme */

body {
  background: linear-gradient(180deg, #1b1530 0%, #2b2147 45%, #3a2d5f 100%);
  color: #f5efff;
}

a {
  color: #ffdff5;
  text-decoration: none;
}

a:hover {
  color: #dff4ff;
  text-shadow: 0 0 6px rgba(223, 244, 255, 0.6);
}

.nick-space-wrap {
  max-width: 1050px;
  margin: 20px auto;
  padding: 24px;
  border: 1px solid rgba(235, 220, 255, 0.35);
  border-radius: 22px;
  background:
    radial-gradient(circle at 20% 20%, rgba(255,255,255,0.10) 0 1px, transparent 2px),
    radial-gradient(circle at 80% 30%, rgba(255,240,250,0.12) 0 1px, transparent 2px),
    radial-gradient(circle at 30% 70%, rgba(215,235,255,0.12) 0 1px, transparent 2px),
    linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.03));
  box-shadow: 0 0 20px rgba(180, 155, 255, 0.18);
}

.nick-space-header {
  text-align: center;
  padding: 12px 12px 20px;
  border-bottom: 1px solid rgba(255,255,255,0.15);
}

.nick-space-title {
  font-size: 34px;
  letter-spacing: 2px;
  color: #fff0fb;
  text-shadow: 0 0 10px rgba(255, 220, 248, 0.35);
}

.nick-space-subtitle {
  margin-top: 8px;
  font-size: 14px;
  color: #e6d8ff;
}

.nick-space-nav {
  text-align: center;
  padding: 14px;
  color: #eadfff;
}

.nick-space-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
  margin-top: 10px;
}

.nick-space-box {
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(245, 228, 255, 0.22);
  border-radius: 18px;
  padding: 18px;
  box-shadow: inset 0 0 14px rgba(255,255,255,0.03);
}

.nick-space-box h2 {
  margin-top: 0;
  color: #ffe4fa;
  font-size: 22px;
}

.nick-space-box p,
.nick-space-box li {
  line-height: 1.7;
  color: #f5efff;
}

.nick-space-footer {
  margin-top: 18px;
  text-align: center;
  padding-top: 18px;
  border-top: 1px solid rgba(255,255,255,0.15);
  color: #eadfff;
  letter-spacing: 1px;
}

/* optional responsive adjustment */
@media (max-width: 700px) {
  .nick-space-grid {
    grid-template-columns: 1fr;
  }

  .nick-space-title {
    font-size: 26px;
  }
}
